Act as a Full Stack Web Developer. Using the following Contract Table [Insert Table Above], generate an Express.js route in server.js and a corresponding register.js frontend script. The logic must perform frontend validation for passwords (min 8 chars, 1 uppercase, 1 special char), use fs to check for existing usernames in auth_user.json on the backend, hash the password using bcrypt, and store the final user object back into the JSON file. Ensure proper error handling and a 2-second redirect to login.html upon success.