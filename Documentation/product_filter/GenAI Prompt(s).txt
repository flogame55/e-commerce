"Using the API Contract and Sequence Map, create a GET route in server.js for /api/products/filter. Implement a Gatekeeper middleware that validates the category query parameter against: ['Vegetables', 'Fruits', 'Juice', 'Dried', 'Hat', 'All']. If valid, query the products table in database.db and return the rows as a JSON package with status 200. Ensure the database connection is closed after the query. This route must be placed before the 404 handler."